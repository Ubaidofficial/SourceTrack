[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd /app/dashboard && npm install"]

[phases.build]
cmds = ["cd /app/dashboard && npm run build"]

[start]
cmd = "cd /app/dashboard && npx serve -s dist -l $PORT"
