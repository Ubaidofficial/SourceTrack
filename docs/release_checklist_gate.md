# SourceTrack Release Checklist Gate — Session 139K

**Engineering Control Document**
**Readiness Status:** 🚨 **NOT READY FOR PAID-BETA RELEASE** (Blocked by open P0/P1 items)

This checklist serves as the canonical release gate. No paid-beta launch, paid-beta readiness claim, or production release approval may occur until all P0 conditions are explicitly closed with verified evidence.

---

## 1. Release Readiness Summary

| Milestone | Target / Ceiling | Required Gates | Status |
|---|---|---|---|
| **Staging / QA Testing** | Internal only | Staging Supabase provisioned, local boot guard active | ✅ **ACTIVE** |
| **Tiny Paid Beta** | 3–5 users / 10 max | All **P0** items Closed | 🚨 **BLOCKED** |
| **Public Launch / Scaling** | Self-serve public | All **P1** items Closed | 🚨 **BLOCKED** |
| **High-Volume / Enterprise** | Large shops / horizontal scaling | All **P2** items Closed | 🚨 **BLOCKED** |

---

## 2. Canonical Readiness Checklist

### Phase 0: Safe Engineering Foundation (Local & Process)
- [x] **Staging Environment Setup**: Separate Supabase staging project created (ref: `nrsvpwzekfrdrzkoecfk`). Local environment rewired to target staging instead of production.
- [x] **Local Boot Guard**: Reusable environment safety boot guard (`api/lib/environment-safety.js`) implemented and executed early via `api/bootstrap.js`. Server refuses to boot locally if `SUPABASE_URL` contains the production ref (`zxjjjsipafojhzkkumvh`).
- [x] **No-Commit-Before-Review Rule**: Codified in `docs/ai_agent_workflow_rules.md`. No commits/pushes can be run by AI agents before raw diff review and explicit operator approval.
- [x] **Setup Doctor Diagnostic Chain**: Unified `SetupDoctorCard` console, API reachability ping, snippet-only `st_verify` live pageview test checks, and copy truth audits completed.

### Phase 1: Blocks the First Paying Beta Customer (P0 - CRITICAL)
- [ ] **Staging Schema Bootstrap**: Complete database migrations and seeding on the staging project (`nrsvpwzekfrdrzkoecfk`). Refer to [staging_schema_bootstrap_plan.md](operations/staging_schema_bootstrap_plan.md) for details.<br />*Status:* **PASS**. All database migrations applied and verified successfully. Production deployment remains **PENDING** / **BLOCKED**.
- [ ] **Staging Service-Role Access**: Add the real staging service-role key to gitignored local/staging environment variables.<br />*Status:* **PARTIAL** — local gitignored staging service-role access was used for QA, but canonical staging bootstrap/gate remains **PENDING** / **BLOCKED** until schema/migration state is verified and documented.
- [ ] **Stripe Test Catalog Alignment**: Correct and align Stripe test-mode price IDs ($29/$79/$149+) to prevent stale pricing schema mismatch.<br />*Status:* **PASS** (test-mode aligned). Verified that Starter ($29/mo), Growth ($79/mo), and Scale ($149/mo) plans in Stripe test mode align with price IDs and limits metadata (50k, 150k, 500k pageviews). Production catalog sync remains **PENDING** / **BLOCKED**.
- [ ] **Stripe Test-Mode E2E Verification**<br />*Status:* **PARTIAL / PENDING / BLOCKED**. Staging schema gaps that previously caused fail-open warnings are resolved, and automated payments API + Stripe webhook ingestion checks now pass without missing database-object warnings. Full hosted checkout browser load, billing portal, and cancel/downgrade E2E still require fresh raw proof before this gate can close.
- [ ] **Supabase Backups & PITR Verification**: Confirm backup configuration in the Supabase console. PITR is NOT enabled on the production instance (`zxjjjsipafojhzkkumvh`). PITR is not enabled and remains an open risk unless explicitly accepted by the operator. Do not mark this gate closed without either enabling PITR with cost approval or documenting explicit operator acceptance. Daily backups are verified by the operator for June 3-10, 2026. A staging restore drill must be performed. Refer to [supabase_backup_restore_runbook.md](operations/supabase_backup_restore_runbook.md) for details.<br />*Status:* **BLOCKED**. Staging restore drill not completed.
- [ ] **Production Env/Secrets Verification**: Verify production environment variables (`NODE_ENV`, `ST_IP_RESOLVER_MODE=railway`, `ST_LOG_HASH_SECRET`, `TRACKER_SALT`, `ALLOWED_ORIGINS`) are set correctly in the Railway console.<br />*Status:* **PARTIAL (PENDING/BLOCKED)**. Production Env/Secrets Verification is partially resolved: production/staging service env separation was audited with safe evidence (140G-20), `TRACKER_SALT` blocker is resolved, staging `NODE_ENV` is changed to staging (140G-21), and PostHog staging/production separation is verified **PASS** (140G-24B). Staging and production use separate PostHog projects (`469905` for staging and `416017` for production), with separate keys. Test-event isolation is verified via runtime capture queries. Production API `POSTHOG_API_KEY` warning remains open (personal/query key and needs runtime capture verification on production). Gate remains open for missing/unverified production variables, live Stripe mode/E2E proof, and final production smoke verification. See [production_env_secrets_verification_140G-20.md](qa/production_env_secrets_verification_140G-20.md), [env_blocker_fix_tracker_id_smoke_140G-21.md](qa/env_blocker_fix_tracker_id_smoke_140G-21.md), and [posthog_staging_project_isolation_140G-24B.md](qa/posthog_staging_project_isolation_140G-24B.md).
- [ ] **Billing/Limits Enforcement Audit**: Audit all billing/limit checking middleware to ensure free and active-tier users cannot exceed their monthly thresholds without payment.<br />*Status:* **PARTIAL (PENDING/BLOCKED)**. D17 code audit found no obvious implementation blocker in Stripe checkout, webhook, portal, and plan/limit mapping paths. However, live-mode separation and production Stripe configuration (live webhooks, live price IDs, customer portal settings) require manual operator setup and remain blocked. Full live execution proof is required before gate closure. See `docs/archive/qa/billing_stripe_production_readiness_140Z-G3-D17.md`.
- [ ] **Production Observability**: Confirm logging levels, dashboard alerts, and key telemetry paths are operational in production.<br />*Status:* **PARTIAL (PENDING/BLOCKED)**. D15 audit confirms backend log safety, request IDs, and health endpoint are production-safe with zero secret leakage. Frontend error capture and proactive external alerting remain absent. Gate remains open pending log retention confirmation and any required incident alerting setup.
- [ ] **Data Deletion & Privacy Basics**: Ensure manual and automated user deletion routines purge associated personal data correctly from the database.<br />*Status:* **PARTIAL (Supabase DB deletion code path audited, proxy/object-level PII sanitization locally implemented and unit-tested; Settings Danger Zone and Privacy copy hardened to be completely truthful in Session 140G-7; PostHog retention/purge operator runbook is documented in Session 140G-8; live PostHog visitor-level erasure successfully verified E2E on staging in Session 140G-11; account/site-level PostHog event purges remain blocked at the API layer but managed via manual runbook checklists)**. Individual visitor erasure code path deletes Supabase attribution rows and stitched identity links, and sends a best-effort delete command to PostHog. Staging E2E deletion drill successfully verified that visitor DB records and PostHog Person profiles (including associated events) are no longer visible after polling in the controlled staging drill. Account deletion code path cascades to purge Supabase workspace data, but leaves historical raw events inside the PostHog project. Proxy routes `/sp/e` and `/sp/c`, standard `/api/track` route, custom inbound webhooks, and legacy collect route are locally hardened with recursive object-level and URL query-parameter PII sanitization and verified by unit tests; production/live customer verification remains pending. PostHog event retention purges and manual/live deletion verification remain pending. See [posthog_retention_deletion_audit_140G-5.md](qa/posthog_retention_deletion_audit_140G-5.md), [pii_sanitization_hardening_plan_140G-6.md](qa/pii_sanitization_hardening_plan_140G-6.md), [privacy_copy_truth_hardening_140G-7.md](qa/privacy_copy_truth_hardening_140G-7.md), [posthog_retention_purge_runbook_140G-8.md](qa/posthog_retention_purge_runbook_140G-8.md), and [posthog_visitor_erasure_verification_140G-11.md](qa/posthog_visitor_erasure_verification_140G-11.md).
- [ ] **Backup/Recovery Drill**: Execute a dry-run database restore drill using staging to validate the recovery path and improve operational recoverability. Refer to [supabase_backup_restore_runbook.md](operations/supabase_backup_restore_runbook.md) for details.<br />*Status:* **BLOCKED**.
- [ ] **End-to-End Install QA**: Manually test snippet installation and end-to-end visitor telemetry routing from a live non-localhost environment.<br />*Status:* **PARTIAL (Session 140Z-G3-D16C verified localtunnel tracker ingestion and conversion visibility; pending durable dummy host and full UTM attribution verification)**.
- [ ] **Full Docs Truth Audit**: Ensure all documentation, troubleshooting steps, and installation instructions accurately match the implemented API and UI features.<br />*Status:* **PARTIAL (Settings Danger Zone and Privacy copy has been hardened in Session 140G-7 to prevent overclaiming deletion behavior; other documentation pages and guides remain pending full audit)**. Other gates remain BLOCKED.
- [ ] **Support Readiness**: Establish support routing pipelines and triage protocols for incoming customer queries.<br />*Status:* **BLOCKED**.
- [ ] **Legal/Policy Readiness**: Publish customer-facing terms of service, privacy policy, and GDPR data processing addenda matching beta terms.<br />*Status:* **BLOCKED**.
- [ ] **Admin/Operator Access**: Audit and verify operator security credentials, database direct access access rules, and control panels.<br />*Status:* **BLOCKED**.
- [ ] **Abuse/Rate-Limit Review**: Review and tune rate-limiting settings on high-volume ingress routes (/api/collect, login, registration) to mitigate automated abuse.<br />*Status:* **PARTIAL (PENDING/BLOCKED)**. Abuse/rate-limit review is partially resolved: public dashboard sharing is protected, Stripe webhook limits remain enforced, proxy/pixel/offline ingestion routes now avoid the generic IP-only default limiter where appropriate, and early-beta in-memory limiters are in place. Multi-instance/distributed rate limiting and load/staging verification remain deferred before broader production scaling.
- [ ] **Customer-Facing Status/Incident Plan**: Document customer-facing incident communications protocol, service status reporting, and contact options.<br />*Status:* **BLOCKED**.
- [ ] **Password Reset & Auth Flow Verification**: Implement and verify that forgot password, recovery link redirects, and password updates work correctly end-to-end. Configure and verify allowed redirect URLs and SMTP templates in the Supabase console.<br />*Status:* **PARTIAL — staging password reset E2E PASS after Supabase Auth URL config fix; production/canonical-domain auth still unverified.** Flow implemented & deployed (139N-4B, `3e41f58`); staging auth routes browser-verified PASS (139N-4C/4D). 139N-4D staging email E2E **initially FAILED** (real, documented): reset request submitted PASS and the Supabase recovery **email delivered PASS**, but the recovery link **redirected to `http://localhost:3000/`** (dead) instead of the deployed `/reset-password`. **Root cause = Supabase Auth URL config, not app code**: app already passes `redirectTo=${origin}/reset-password` and consumes the recovery hash, but the deployed URL was not in the **Auth Redirect URLs allowlist**, so Supabase fell back to **Site URL = `http://localhost:3000`** (dev default). **Fix applied (operator, Supabase console — staging & production are SEPARATE Supabase projects; do NOT mix their URLs):** on the staging project (`nrsvpwzekfrdrzkoecfk`) Site URL = `https://sourcetrack-dashboard-staging.up.railway.app` and Redirect URLs include `/reset-password`,`/auth/callback`,`/login` + staging wildcard + `http://localhost:3000/**`; production URLs (`https://www.sourcetrack.ai/**`) stay ONLY in the production Supabase project. **Operator then manually verified the full staging chain PASS**: fresh reset email → link landed on staging `/reset-password` → password update → login after reset → staging `/dashboard` loaded. Secondary follow-up (139N-4E — RESOLVED): `dashboard/src/lib/supabase.js` storageKey is now dynamically derived from `VITE_SUPABASE_URL` project ref to prevent cross-env session collision. **Production/canonical-domain reset remains UNVERIFIED.** Gate not closed until the same chain passes on production with the production Supabase project's Auth-URL config confirmed.
- [ ] **Piqo-Inspired Simplicity + Setup Value Enhancements**: Address onboarding/install simplicity, tracker/API docs clarity, demo/staging data readiness, and dashboard "aha" moment. (Pre-paid-beta Phase 1 components are hard paid-beta blockers; advanced GSC, Stripe setup, and SEO comparison items are deferred to Phase 2/post-paid-beta). See [piqo_simplicity_setup_value_enhancements.md](piqo_simplicity_setup_value_enhancements.md) for details.<br />*Status:* **BLOCKED / PENDING — roadmap item added; implementation pending**.
- [ ] **Production Auth Route Smoke Test**: Route availability smoke passes, but production Supabase Auth redirect URLs and end-to-end reset/OAuth flows remain unverified until operator configuration is confirmed and browser-tested.<br />*Status:* **BLOCKED — production Supabase Auth Redirect URLs are likely not configured for app.sourcetrack.ai.**

### Phase 2: Blocks the First ~10 Customers / Public Launch (P1)
- [ ] **Attribution Model Deterministic Test Fixtures**: Add deterministic test scenarios for the core attribution models (first-touch, last-touch, linear, U-shaped, W-shaped, time-decay) to protect against regressions.<br />*Status:* **PARTIAL** — deterministic `calculateAttribution` model math is covered by unit tests (`qa:attribution:unit`). Click ID capture/classification hardened for all 12 click IDs with `qa:tracker:unit` tests. Webhook routes now attempt user_id-to-anonymous_id resolution where prior identity links exist (`qa:identity:unit`). Real Stripe/webhook revenue attribution still requires staging E2E with seeded identify + checkout + webhook events.
- [ ] **Exception Monitoring**: Integrate Sentry (or equivalent) exception monitoring on staging and verify alert routing to Slack.<br />*Status:* **BLOCKED**.
- [ ] **Mandatory CI/Pre-Deploy Test Gate**: Add static, identity/billing, tracker, and attribution unit suites to CI or enforce a mandatory local run gate prior to release.<br />*Status:* **PARTIAL**. Mandatory CI regression gate is implemented for static, identity/billing, tracker, and attribution unit suites; GitHub Actions verification remains pending until push.
- [ ] **Branch Protection & PR Review**: Enforce branch protection on `main` requiring at least one approved code review before merging.<br />*Status:* **BLOCKED**.
- [ ] **HogQL Date Param Sanitization**: Harden date inputs in the attribution engine with proper serialization instead of route-level parsing alone.<br />*Status:* **RESOLVED / PENDING post-deploy verification** (Sanitized and validated date inputs in the attribution engine and routes via `serializeHogQLDateRange` and `serializeHogQLDateTime` with strict ISO format and calendar round-trip checks. Verified via unit tests).
- [ ] **Tenant Isolation Scoping Audit**: Secure `/api/jobs/attribution/status` so global job runs cannot be read by standard tenants.<br />*Status:* **RESOLVED / PENDING post-deploy verification** (job status endpoint gated to super_admin, AI Chat HogQL query validator hardened against prompt injection, and journey visitor profile scoping PostHog API fetch disabled and built strictly from local events; verified via 17 unit tests). See docs/archive/qa/tenant_isolation_followup_fixes_140G-13.md.
- [ ] **Stripe Webhook Rate Limiting**: Enforce a rate limiter on the Stripe webhook endpoint to prevent signature-validation floods.<br />*Status:* **RESOLVED / PENDING post-deploy verification** (Stripe webhook rate limiting is implemented and locally unit-verified; local Stripe E2E script is blocked pending seeded test site data; post-deploy verification remains pending).
- [ ] **Billing Redirect Hardening**: Ensure all billing redirect target URLs are allow-listed or server-derived.<br />*Status:* **PASS**. Checkout success/cancel and portal return URLs are strictly validated server-side against allowlisted dashboard/frontend origins (Session 140F). Invalid checkout redirect targets are rejected; invalid portal return targets fall back to safe default. Verified locally via unit tests; post-deploy browser verification pending. Operational caveat: for non-canonical staging or production dashboard domains, ALLOWED_ORIGINS must include the deployed dashboard origin. FRONTEND_URL or DASHBOARD_URL can supply the fallback return target, but that target is only used if its origin is also allowlisted or is one of the canonical defaults. See `docs/archive/qa/billing_redirect_hardening_140F.md`.
- [ ] **Account Deletion PostHog Erase**: Harden account deletion to bulk-erase related visitor events from PostHog.<br />*Status:* **BLOCKED**.
- [ ] **Onboarding Validation Hardening**: Hard subdomains or disposable email addresses return clean `400` errors instead of a `500` crash.<br />*Status:* **BLOCKED**.
- [ ] **Transactional Email Opt-Out**: Verify suppression list/unsubscribe handling for report digests.<br />*Status:* **BLOCKED**.
- [ ] **Attribution Accuracy + Signal Reliability Hardening**: Audit and improve attribution signal quality across browser tracking, custom tracking domain/proxy, server-side conversion API, Stripe webhooks, manual Shopify webhook recipe, UTM capture/persistence, referrer capture, AI referral detection, GSC/search query association, identity stitching, and nightly attribution job behavior. Identify and mitigate signal loss points (ad blockers, Safari/ITP, checkout redirects, missing UTMs, direct-traffic overwrite, duplicates, delayed events, anonymous-to-known stitching failures). Add QA tests and produce "What SourceTrack can and cannot know" honesty docs. Customer-facing wording must stay within approved bounds — never claim "100% accurate", "perfect attribution", "ad-block-proof", or "guaranteed attribution". See `docs/attribution_accuracy_signal_reliability_hardening.md`.<br />*Status:* **PARTIAL / PENDING — source code audit and local deterministic math tests PASS; live/deployed E2E attribution validation is BLOCKED pending operator seeding a staging test fixture.**

### Phase 3: Scaling & Architectural Pre-requisites (P2)
- [ ] **Shared State Store**: Move rate limits and webhook idempotency caches to Redis/Upstash before scaling to multiple server instances.<br />*Status:* **BLOCKED**.
- [x] **Conversion Cap Enforcement**: Enforce conversion ingestion limits backend-side.<br />*Status:* **RESOLVED (conversion caps checked at all ingestion points atomically via PostgreSQL RPC, verified via unit tests; paid beta remains blocked by PostHog retention/purging and the remaining open release gates, including paid billing portal verification, production billing verification, production env/secrets verification, tenant isolation, privacy/deletion, observability, install QA, and docs truth audit.)**. See docs/archive/qa/conversion_cap_enforcement_140G-3.md.
- [ ] **Synchronous Ingestion Bottleneck**: Re-architect `/api/collect` writes to be asynchronous.<br />*Status:* **BLOCKED**.

---

## 3. Operator Verification Playbook

Before marking this project as "paid-beta ready" or "production ready", the operator must execute the verification steps detailed below and document the results here.

### Checklist Verification Command
An automated, offline-safe checker is wired into the codebase. Run:
```bash
node scripts/qa-release-readiness.mjs
```
This script will check:
1. That `docs/release_checklist_gate.md` exists and declares the project is NOT ready.
2. That all blocked/open P0/P1 items in the release checklist are explicitly flagged as `[ ]` (unchecked) and `BLOCKED` or `PENDING`.
3. That the environment safety guards remain active.
