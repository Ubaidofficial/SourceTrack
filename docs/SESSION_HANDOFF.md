## Last completed: T4.2 — Public Share Link ✅
- public_share_token + public_share_enabled in sites table
- /api/routes/public-dashboard.js — GET /api/public/:token (no auth)
- Settings.jsx toggle + copy link — fully built
- PublicDashboard.jsx — /public/:token frontend page
- App.jsx route wired

## Next: T4.3 — Live Visitors Counter
live.js already exists and is registered at /api/live
Dashboard.jsx already polls it every 30s and shows count in header
STATUS: T4.3 may already be DONE — verify the live count shows in dashboard header
If working: skip to T4.4 (Custom Tracking Domain)
