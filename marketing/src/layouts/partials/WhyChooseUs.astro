---
import IconBox from "@/components/IconBox.astro";
import SectionHeader from "@/components/SectionHeader.astro";
interface Props {
  data: any;
}
const { data } = Astro.props;
const { badge, title, feature_list, enable } = data.frontmatter;
---

{enable && (
  <section>
    <div class="main-container">
      <div class="container">
        <div class="container-padding-y container-padding-x">
          <SectionHeader title={title} badge={badge} />
          <div class="grid md:grid-cols-2 gap-8 md:gap-14 lg:gap-25 relative overflow-hidden">
            {feature_list.map((item: any) => {
              const uid = `wcu-${Math.random().toString(36).slice(2, 8)}`;
              return (
                <div id={uid} style="opacity:0">
                  <IconBox icon={item.icon} />
                  <h3 class="text-h5 font-medium mb-5">{item.title}</h3>
                  <p class="text-gray">{item.content}</p>
                </div>
              );
            })}
            <!-- Dividers: one per axis at the 2x2 midpoint. Colour is the border token
                 (#E5E7EB) — the template drew these in white at 8-10% alpha, which is
                 invisible against the #F5F4F0 body. The gradient keeps the original fade-out
                 at the ends. -->
            <div
              class="hidden md:block absolute inset-y-0 left-1/2 w-px -translate-x-1/2"
              style="background: linear-gradient(to bottom, transparent 0%, var(--color-border) 35%, var(--color-border) 65%, transparent 100%)"
              aria-hidden="true"
            >
            </div>
            <div
              class="hidden md:block absolute inset-x-0 top-1/2 h-px -translate-y-1/2"
              style="background: linear-gradient(to right, transparent 0%, var(--color-border) 20%, var(--color-border) 80%, transparent 100%)"
              aria-hidden="true"
            >
            </div>

            <!-- Star at the single intersection the 2x2 grid now has -->
            <div class="hidden md:block absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2" aria-hidden="true">
              <svg width="61" height="61" viewBox="0 0 61 61" fill="none" xmlns="http://www.w3.org/2000/svg">
                <path d="M30.5 0C30.5 11.8843 49.1169 30.5 61 30.5C49.1169 30.5 30.5 49.118 30.5 61C30.5 49.118 11.8831 30.5 0 30.5C11.8831 30.5 30.5 11.8843 30.5 0Z" class="fill-body"/>
                <path d="M30.5 3.67676C31.0276 5.73701 31.95 7.886 33.1592 10.0254C35.116 13.4875 37.8441 16.9643 40.9404 20.0605C44.0366 23.1566 47.5137 25.8842 50.9756 27.8408C53.1148 29.0498 55.2633 29.9724 57.3232 30.5C55.2633 31.0276 53.1147 31.9501 50.9756 33.1592C47.5137 35.116 44.0367 37.8442 40.9404 40.9404C37.8442 44.0368 35.116 47.5136 33.1592 50.9756C31.9501 53.1147 31.0277 55.2634 30.5 57.3232C29.9723 55.2634 29.0499 53.1147 27.8408 50.9756C25.884 47.5136 23.1558 44.0368 20.0596 40.9404C16.9633 37.8442 13.4863 35.116 10.0244 33.1592C7.88497 31.9499 5.73598 31.0276 3.67578 30.5C5.73602 29.9724 7.88493 29.05 10.0244 27.8408C13.4863 25.8842 16.9634 23.1566 20.0596 20.0605C23.1559 16.9643 25.884 13.4875 27.8408 10.0254C29.05 7.886 29.9724 5.73701 30.5 3.67676Z" stroke="var(--color-border)"/>
              </svg>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>
)}

<script>
  import { animateMini as animate, inView } from "motion";
  function initWhyChooseUs() {
    document.querySelectorAll<HTMLElement>('[id^="wcu-"]').forEach((el, i) => {
      el.style.opacity = "0";
      inView(el, () => {
        animate(el, { opacity: [0, 1], transform: ["translateY(20px)", "translateY(0)"] }, { duration: 0.5, ease: "easeInOut", delay: i * 0.1 });
      }, { margin: "0px", amount: 0.05 });
    });
  }
  document.addEventListener("astro:page-load", initWhyChooseUs);
</script>
