---
// SourceTrack Visitor Journey panel — the SECOND product surface on this page, alongside
// DashboardMockup.astro (the hero's Attribution card). Same technique on purpose: markup
// ported from the real component rather than a PNG, so the labels stay greppable, the
// chrome inherits real brand tokens, and it cannot silently drift out of date the way a
// screenshot does.
//
// Ported from dashboard/src/components/JourneyModal.jsx — the two-column layout (lime-tinted
// profile rail + session timeline), the attribution-trail chip strip (§8.6:
// [Source] -> [Landing] -> [Event] -> [Revenue]), the qualified check overlaid on the
// avatar, and the All / Conversions / AI Touches filter pills are all that component's.
//
// Two invariants carried over from the real UI rather than re-invented for marketing:
//   • The avatar is a MONO GLYPH off the visitor's short id — never a name, never a photo.
//     There is no person-level identity to draw here, and inventing one would contradict
//     the cookieless / no-de-anonymisation rule (§6).
//   • Revenue renders only where a real value exists. The sessions that did not convert
//     show no revenue at all — not "$0", not a dash (§6: no fake zeros).
//
// EVERY NUMBER HERE IS ILLUSTRATIVE and the card says so in its own header badge. It is a
// drawing of the product, not a claim about anyone's data.
const trail = [
  { label: "ChatGPT", tone: "default" },
  { label: "/compare", tone: "mono" },
  { label: "$conversion", tone: "mono" },
  { label: "$240", tone: "revenue" },
];

const stats = [
  { label: "Sessions", value: "3" },
  { label: "Conversions", value: "1" },
  { label: "Revenue", value: "$240" },
  { label: "Duration", value: "6d" },
];

const sessions = [
  {
    index: 1,
    source: "ChatGPT",
    when: "Jul 12, 09:14",
    duration: "2m 41s",
    converted: false,
    events: ["/compare", "/pricing"],
  },
  {
    index: 2,
    source: "Direct",
    when: "Jul 15, 18:02",
    duration: "1m 12s",
    converted: false,
    events: ["/docs/install"],
  },
  {
    index: 3,
    source: "Google Ads",
    when: "Jul 18, 11:27",
    duration: "4m 08s",
    converted: true,
    events: ["/pricing", "Checkout completed"],
  },
];
---

<div class="relative mx-auto w-full max-w-[940px]">
  <div
    class="absolute left-1/2 top-[40px] h-[360px] w-[520px] -translate-x-1/2 rounded-full bg-primary opacity-[0.18] blur-[64px]"
    aria-hidden="true"
  >
  </div>

  <div
    class="relative rounded-[28px] border border-white/10 bg-[#0D1010] p-3 shadow-[0_30px_90px_rgba(31,35,35,.30)]"
  >
    <div class="overflow-hidden rounded-[20px] border border-[#2D3333] bg-[#121616]">
      <!-- Window chrome -->
      <div
        class="grid h-[48px] grid-cols-[80px_1fr_auto] items-center gap-2 border-b border-white/10 bg-[#171B1B] px-4 text-xs font-bold text-[#F5F8F8]"
      >
        <div class="flex gap-[6px]">
          <span class="h-2.5 w-2.5 rounded-full bg-[#C4381C]"></span>
          <span class="h-2.5 w-2.5 rounded-full bg-[#FF7A33]"></span>
          <span class="h-2.5 w-2.5 rounded-full bg-primary"></span>
        </div>
        <span class="truncate">SourceTrack — Visitor Journey</span>
        <span
          class="inline-flex items-center rounded-full bg-primary/10 px-2.5 py-1 text-[10px] font-black uppercase tracking-wider text-primary"
          >Sample data</span
        >
      </div>

      <div class="grid gap-px bg-[#2D3333] md:grid-cols-5">
        <!-- ── Profile rail ── -->
        <div class="space-y-4 bg-[#141918] p-4 md:col-span-2">
          <div>
            <p class="text-[10px] uppercase tracking-wider text-[#9DA7A7]">Profile</p>
            <div class="mt-2 flex items-center gap-2.5">
              <div class="relative flex-shrink-0">
                <div
                  class="flex h-10 w-10 items-center justify-center rounded-full border border-primary/40 bg-primary/15"
                >
                  <span class="font-mono text-xs font-bold tracking-tight text-white">A4</span>
                </div>
                <!-- Qualified badge — an ABSENT badge means "not qualified", so it is never a default -->
                <span
                  class="absolute -bottom-0.5 -right-0.5 flex h-4 w-4 items-center justify-center rounded-full bg-[#22C55E] ring-2 ring-[#141918]"
                  title="Qualification: QUALIFIED"
                >
                  <svg viewBox="0 0 24 24" class="h-2.5 w-2.5" fill="none" stroke="white" stroke-width="4">
                    <path d="M20 6L9 17l-5-5" stroke-linecap="round" stroke-linejoin="round"></path>
                  </svg>
                </span>
              </div>
              <div class="min-w-0">
                <h3 class="break-all font-mono text-base font-bold leading-tight text-white">a4f2c9e1</h3>
                <p class="mt-0.5 text-[10px] text-[#9DA7A7]">Anonymous visitor · no cookie set</p>
              </div>
            </div>
          </div>

          <!-- Attribution trail -->
          <div class="rounded-xl border border-[#2D3333] bg-[#181D1D] p-3">
            <p class="mb-2 text-[10px] uppercase tracking-wider text-[#9DA7A7]">Attribution trail</p>
            <div class="flex flex-wrap items-center gap-1">
              {
                trail.map((chip, i) => (
                  <span class="flex items-center gap-1">
                    <span
                      class:list={[
                        "inline-flex max-w-[110px] items-center rounded-md border px-1.5 py-0.5 text-[10px] font-medium",
                        chip.tone === "revenue"
                          ? "border-primary/40 bg-primary/15 text-primary"
                          : "border-[#3A4040] bg-[#202625] text-[#DDE5E5]",
                        chip.tone === "mono" ? "font-mono" : "",
                      ]}
                    >
                      <span class="truncate">{chip.label}</span>
                    </span>
                    {i < trail.length - 1 && (
                      <svg viewBox="0 0 24 24" class="h-2.5 w-2.5 flex-shrink-0 text-[#5A6363]" fill="none" stroke="currentColor" stroke-width="3">
                        <path d="M5 12h14M13 6l6 6-6 6" stroke-linecap="round" stroke-linejoin="round" />
                      </svg>
                    )}
                  </span>
                ))
              }
            </div>
          </div>

          <!-- KPI grid -->
          <div class="grid grid-cols-2 gap-2">
            {
              stats.map((s) => (
                <div class="rounded-xl border border-[#2D3333] bg-[#181D1D] p-2.5 text-center">
                  <p class="truncate text-lg font-bold text-white">{s.value}</p>
                  <p class="mt-0.5 text-[10px] uppercase tracking-wider text-[#9DA7A7]">{s.label}</p>
                </div>
              ))
            }
          </div>
        </div>

        <!-- ── Session timeline ── -->
        <div class="bg-[#121616] p-4 md:col-span-3">
          <div class="mb-3 flex flex-wrap items-center gap-2">
            <span class="rounded-full bg-white px-3 py-1 text-[11px] font-medium text-[#121616]">All (5)</span>
            <span class="rounded-full bg-[#202625] px-3 py-1 text-[11px] font-medium text-[#9DA7A7]">Conversions (1)</span>
            <span class="rounded-full bg-[#202625] px-3 py-1 text-[11px] font-medium text-[#9DA7A7]">AI Touches (1)</span>
          </div>

          <div class="space-y-2">
            {
              sessions.map((s) => (
                <div
                  class:list={[
                    "rounded-xl border p-3",
                    s.converted ? "border-primary/40 bg-primary/8" : "border-[#2D3333] bg-[#181D1D]",
                  ]}
                >
                  <div class="flex items-start gap-3">
                    <div
                      class:list={[
                        "mt-0.5 flex h-7 w-7 flex-shrink-0 items-center justify-center rounded-full text-[10px] font-bold",
                        s.converted ? "bg-primary text-[#121616]" : "bg-[#2D3333] text-[#DDE5E5]",
                      ]}
                    >
                      #{s.index}
                    </div>

                    <div class="min-w-0 flex-1">
                      <div class="flex flex-wrap items-center gap-2">
                        <span class="text-xs font-semibold text-[#F5F8F8]">Session {s.index}</span>
                        <span class="truncate rounded bg-[#2D3333] px-1.5 py-0.5 text-[10px] font-medium text-[#9DA7A7]">
                          {s.source}
                        </span>
                        {s.converted && (
                          <span class="inline-flex items-center gap-1 rounded-full bg-primary/20 px-1.5 py-0.5 text-[10px] font-medium text-primary">
                            <svg viewBox="0 0 24 24" class="h-2.5 w-2.5" fill="currentColor">
                              <path d="M13 2L3 14h7l-1 8 10-12h-7l1-8z" />
                            </svg>
                            Converted
                          </span>
                        )}
                      </div>
                      <div class="mt-1 flex flex-wrap items-center gap-2 text-[10px] text-[#7E8888]">
                        <span>{s.when}</span>
                        <span>·</span>
                        <span>{s.duration}</span>
                      </div>

                      <div class="mt-2 space-y-1">
                        {s.events.map((e) => (
                          <div class="flex items-center gap-2 text-[11px] text-[#DDE5E5]">
                            <span class="h-1 w-1 flex-shrink-0 rounded-full bg-[#5A6363]" />
                            <span class="truncate font-mono">{e}</span>
                          </div>
                        ))}
                      </div>
                    </div>

                    {/* Revenue shows ONLY where a real value exists — the two non-converting
                        sessions render nothing here rather than "$0" or a dash (§6). */}
                    {s.converted && <span class="flex-shrink-0 text-sm font-black text-primary">$240</span>}
                  </div>
                </div>
              ))
            }
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
